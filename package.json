{
  "name": "whatsapp-auto-reply-saas",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "lint": "node scripts/lint.js",
    "test": "node --test",
    "build": "node scripts/build.js",
    "check": "npm run lint && npm test && npm run build"
  },
  "engines": {
    "node": ">=20"
  }
}
